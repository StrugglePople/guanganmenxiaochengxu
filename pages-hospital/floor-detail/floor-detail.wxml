<!--pages/into-hospital/floor-detail/floor-detail.wxml-->
<view wx:for="{{detail}}" class="floor-list">
    <view class="floor">{{detail.floor}}</view>
    <view class="description">{{detail.description}}</view>
</view>
